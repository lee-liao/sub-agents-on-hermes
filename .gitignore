# Claude Code local-only settings (machine-specific permission allowlists)
.claude/settings.local.json

# Claude Code skill definitions (local tooling, not project content)
.claude/

# BMad framework install and generated output (local tooling, not project content)
_bmad/
_bmad-output/

# Python build/test artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# golden_session runtime state (never commit secrets / transcripts / registry)
.gs-locks/
registry.json

# Secrets — never commit
.env
.env.*
!.env.example

# intermediate files
output/
